import { StyleSheet } from 'react-native'

export const loginCardStyles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontSize: 20,
    fontWeight: '600',
    lineHeight: 30,
    marginBottom: 10
  },
  marginBottom: {
    marginBottom: 10
  }
})
