import { StyleSheet } from 'react-native'

export const cardStyles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    borderColor: '#000',
    borderWidth: 1,
    borderRadius: 10,
    paddingHorizontal: 20,
    paddingVertical: 10
  }
})
