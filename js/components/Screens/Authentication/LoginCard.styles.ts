import { StyleSheet } from 'react-native'

export const loginCardStyles = StyleSheet.create({
  title: {
    textAlign: 'center',
    marginBottom: 10
  },
  marginBottom: {
    marginBottom: 10
  }
})
