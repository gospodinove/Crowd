export type ThemeT = {
  colors: {
    primary: string
    background: string
    secondaryBackground: string
    text: string
    border: string
    icon: string
    black: '#000'
    white: '#fff'
    grey: 'grey'
  }
}
