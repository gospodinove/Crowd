export type ThemeT = {
  colors: {
    primary: string
    background: string
    card: string
    text: string
    border: string
    notification: string
    secondaryBackground: string
    icon: string
    black: '#000'
    white: '#fff'
  }
}
