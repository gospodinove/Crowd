export type ThemeT = {
  colors: {
    primary: string
    background: string
    secondaryBackground: string
    text: string
    border: string
    notification: string
    icon: string
    black: '#000'
    white: '#fff'
  }
}
